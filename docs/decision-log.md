# 项目决策日志

> 更新日期：2026-07-21

本文件记录会影响产品范围、数据契约、技术路线或执行方式的重要选择。状态包括“已接受”“暂定”“部分替代”“已替代”和“已拒绝”。改变既有决定时新增记录并引用被替代编号，不删除历史。

## 决策索引

| 编号 | 决策 | 状态 | 日期 |
| --- | --- | --- | --- |
| D-001 | V1 只使用盘口市场数据 | 已接受 | 2026-07-15 |
| D-002 | 预测口径为常规时间 90 分钟及补时 | 已接受 | 2026-07-15 |
| D-003 | 使用四个固定预测切点 | 已接受 | 2026-07-15 |
| D-004 | 原始数据 append-only 并保留四类时间 | 已接受 | 2026-07-15 |
| D-005 | 采用严格的 observed-at 防泄漏规则 | 已接受 | 2026-07-15 |
| D-006 | MVP 基础技术栈 | 已接受 | 2026-07-15 |
| D-007 | 预算、部署地区和告警方式 | 部分替代 | 2026-07-15 |
| D-008 | 当前采用单总控 Agent | 已接受 | 2026-07-15 |
| D-009 | 个人研究数据获取范围与来源组合 | 部分替代 | 2026-07-15 |
| D-010 | 历史回抓数据与严格回测资格分层 | 已接受 | 2026-07-15 |
| D-011 | 500 作为前瞻研究主数据源 | 已接受 | 2026-07-15 |
| D-012 | 采集全部竞彩比赛并分层准入 | 已接受 | 2026-07-15 |
| D-013 | 本地 Windows 完成首轮运行验证 | 部分替代 | 2026-07-15 |
| D-014 | 保存竞彩 SP 但不纳入 V1 模型 | 已接受 | 2026-07-15 |
| D-015 | 提前进入阶段 3 标准化数据库 | 已接受 | 2026-07-16 |
| D-016 | 准备迁移到阿里云杭州单机 ECS | 部分替代 | 2026-07-16 |
| D-017 | 采用实际 ECS 并分离 smoke 与正式存储 | 部分替代 | 2026-07-16 |
| D-018 | 暂缓购买 ECS 数据盘并维持本地正式运行 | 已接受 | 2026-07-16 |
| D-019 | 采用全自动赛果确认与保守隔离 | 部分替代 | 2026-07-17 |
| D-020 | Windows S4U 与 G 盘双层异盘备份 | 部分替代 | 2026-07-17 |
| D-021 | 数据库任务采用本机加密密码登录兼容路径 | 已接受 | 2026-07-17 |
| D-022 | 允许隔离的公开历史研究与离线市场基线 | 已接受 | 2026-07-17 |
| D-023 | 盘口标准化 V2 与模型资格分层 | 已接受 | 2026-07-17 |
| D-024 | 赛果补偿采用赛事 ID 口径登记与分析页双端点 fallback | 已接受 | 2026-07-20 |
| D-025 | 无效场次采用追加式逻辑排除 | 已接受 | 2026-07-20 |
| D-026 | 接入中国体彩官方 90 分钟赛果证据源 | 已接受 | 2026-07-20 |
| D-027 | 体彩官方赛果采用低频调度与失败证据隔离 | 部分替代 | 2026-07-20 |
| D-028 | 允许项目负责人直接确认现有候选的90分钟口径 | 已接受 | 2026-07-20 |
| D-029 | 允许隔离盘口影子预测 | 已接受 | 2026-07-20 |
| D-030 | 赛事类型与市场证据等级分离 | 已接受 | 2026-07-21 |
| D-031 | 韩职 K1 规则护栏采用 shadow 优先和人工启用 | 已接受 | 2026-07-21 |

## D-001：V1 只使用盘口市场数据

- 日期：2026-07-15
- 状态：已接受
- 背景：需要验证仅依靠市场信息能否形成可校准、可复现的概率预测。
- 决定：V1 输入限定为欧赔、亚洲让球、大小球、多公司盘口变化和相关时间信息。
- 理由：控制变量和数据范围，优先解决数据质量、时间语义及防泄漏问题。
- 影响：球队新闻、伤停、阵容、天气、舆情和人工观点不进入 V1 模型。
- 替代方案：多源球队基本面模型；留待 V1 基线稳定后另行决策。

## D-002：预测口径为常规时间 90 分钟及补时

- 日期：2026-07-15
- 状态：已接受
- 背景：杯赛可能包含加时和点球，必须统一标签口径。
- 决定：所有预测和结算默认只计算常规时间 90 分钟及补时。
- 理由：与主流 1X2 常规时间市场保持一致，避免混合不同结算规则。
- 影响：加时和点球不得用于胜平负标签；赛果源必须能区分常规时间结果。
- 替代方案：预测晋级结果；不属于当前产品范围。

## D-003：使用四个固定预测切点

- 日期：2026-07-15
- 状态：已接受
- 背景：需要比较不同临场阶段并冻结可追溯版本。
- 决定：使用 `T-24h`、`T-6h`、`T-60m`、`T-10m` 四个切点。
- 理由：兼顾早期判断、赛前变化和临场状态。
- 影响：采集频率、新鲜度、快照和回测均围绕四个切点设计。
- 替代方案：连续实时预测；首版复杂度过高，暂不采用。

## D-004：原始数据 append-only 并保留四类时间

- 日期：2026-07-15
- 状态：已接受
- 背景：供应商可能更新或事后修正盘口记录，需要保留完整历史。
- 决定：原始响应只追加，并保存 `source_event_time`、`observed_at`、`ingested_at`、`corrected_at`。
- 理由：支持审计、重放和识别事后修正。
- 影响：供应商修正不能覆盖原始记录或已经发布的预测。
- 替代方案：只保存最新状态；无法可靠复现，拒绝采用。

## D-005：采用严格的 observed-at 防泄漏规则

- 日期：2026-07-15
- 状态：已接受
- 背景：数据源声明时间可能早于系统实际获得数据的时间。
- 决定：预测和回测只能使用 `observed_at <= prediction_cutoff` 的数据。
- 理由：确保离线回测不会使用当时系统尚未看到的信息。
- 影响：所有快照、特征、公司权重和训练切分都必须接受时间审计。
- 替代方案：只按供应商事件时间过滤；存在未来数据泄漏风险，拒绝采用。

## D-006：MVP 基础技术栈

- 日期：2026-07-15
- 状态：已接受
- 背景：项目需要可维护的采集、建模、API 和可视化能力。
- 决定：MVP 采用 Python、FastAPI、PostgreSQL、独立采集 Worker、React、ECharts 和 Docker Compose。
- 理由：技术成熟，适合单用户私有系统，并能覆盖数据和 Web 工作流。
- 影响：暂不默认引入 TimescaleDB、Redis 或 Celery，达到明确需求后再决策。
- 替代方案：单体脚本或更复杂的分布式架构；均不作为 MVP 默认方案。

## D-007：预算、部署地区和告警方式

- 日期：2026-07-15
- 状态：部分替代，部署地区被 D-016 替代
- 背景：执行计划需要一个可估算的运行环境，但尚未获得最终人工确认和供应商报价。
- 决定：暂按每月不超过 CNY 300、香港或新加坡 Ubuntu VPS、email 告警规划。
- 理由：为早期方案提供保守默认值。
- 影响：这些值不能作为采购授权，也不能用于降低数据验收标准。
- 替代方案：本地常驻、其他云地区或其他告警渠道；获得实际需求后再比较。

## D-008：当前采用单总控 Agent

- 日期：2026-07-15
- 状态：已接受
- 背景：项目仍在数据源调研期，立即维护多个角色 Agent 会增加交接和同步成本。
- 决定：使用根目录 `AGENTS.md` 作为单总控 Agent 契约，按项目阶段切换数据审计、数据工程、模型验证和产品运维职责。
- 理由：保持唯一状态入口和阶段门禁，同时为后续拆分保留清晰职责边界。
- 影响：当前不创建角色专属 Agent 文件或自定义 Skill。
- 替代方案：立即建立多角色 Agent；待并行开发需求实际出现时重新评估。

## D-009：个人研究数据获取范围与来源组合

- 日期：2026-07-15
- 状态：已接受，主数据源定位被 D-011 部分替代
- 背景：用户希望在不考虑商业授权和收费的个人学习研究前提下，先获取 2025 年至当前时间的数据进行探索。
- 决定：研究数据范围为 `2025-01-01T00:00:00Z` 至每次执行时的真实时间；免费来源组合暂定为 Football-Data、OddsHarvester 和 FlashscoreScraper，具体步骤见 `docs/research-data-acquisition-plan.md`。
- 理由：该组合能覆盖赛果、开盘/收盘基线、历史盘口研究和少量人工交叉验证，适合先验证字段结构和覆盖风险。
- 影响：可以执行范围受限的研究数据下载和小样本验证；该决定不代表正式数据源授权验收通过，也不允许跳过阶段 1。
- 替代方案：直接购买授权 API；在正式产品验收中仍需评估，但当前研究阶段暂不执行。

## D-010：历史回抓数据与严格回测资格分层

- 日期：2026-07-15
- 状态：已接受
- 背景：历史回抓数据的真实观察时间晚于比赛发生时间，若直接用于 observed-at 回测会产生未来数据泄漏。
- 决定：历史回抓数据必须标记 `backfill=true`，`observed_at` 使用真实下载时间，`strict_backtest_eligible=false`；只有从现在开始前瞻采集且满足防泄漏条件的数据才可标记为严格回测可用。
- 理由：保留历史研究价值，同时不破坏项目的 observed-at 防泄漏原则。
- 影响：历史回抓数据可用于覆盖研究、字段验证和非严格分析，但不能用于声称严格模拟当时可见信息的回测。
- 替代方案：按数据源声明时间直接参与严格回测；存在泄漏风险，拒绝采用。

## D-011：500 作为前瞻研究主数据源

- 日期：2026-07-15
- 状态：已接受
- 背景：500 竞彩页面能够发现带稳定 fixture ID 的比赛，500 分析页已验证可取得欧赔、亚盘、大小球和让球指数；用户愿意从当前时间连续积累 1 至 2 年。
- 决定：个人研究阶段使用 500 作为前瞻采集主数据源，付费或授权 API 调研降为未来可选项；本决定部分替代 D-009 的主来源定位，但不删除既有历史研究数据。
- 理由：前瞻真实观察能够建立严格 `observed_at` 时间线，并避免依赖不完整历史回填。
- 影响：当前阶段门禁改为 500 技术验收；7 天和 30 天验收通过前仍不开发数据库、模型或 Web 产品。
- 替代方案：继续等待 2 至 4 个授权 API；不再作为当前阻塞。

## D-012：采集全部竞彩比赛并分层准入

- 日期：2026-07-15
- 状态：已接受
- 背景：固定赛事白名单会漏掉竞彩页面上的其他比赛，也降低长期样本积累速度。
- 决定：采集六个足球竞彩玩法页面中所有 fixture ID 的并集，不设联赛白名单；训练、回测和发布资格按盘口、时间、身份和赛果质量单独判断。
- 理由：数据保存范围和模型适用范围是不同问题，广泛保存不会自动授权低质量比赛进入模型。
- 影响：所有质量指标按赛事分组；来源缺盘与程序失败必须分开统计。
- 替代方案：只采五大联赛、欧战、世界杯和 K1；拒绝作为采集边界，保留为重点评估分组。

## D-013：本地 Windows 完成首轮运行验证

- 日期：2026-07-15
- 状态：部分替代，云端准备见 D-016
- 背景：当前开发环境和既有数据均位于 Windows，本阶段优先验证数据闭环而非部署架构。
- 决定：首版使用本地 Windows 任务计划程序运行，完成 7 天技术验收和 30 天稳定性验收后再评估 VPS。
- 理由：复用现有环境，缩短开始前瞻积累的时间。
- 影响：人工必须保证联网、不休眠和时钟准确；30 天验收前必须配置异盘或网络备份。
- 替代方案：从第一天部署 Linux VPS；留待季度复盘。

## D-014：保存竞彩 SP 但不纳入 V1 模型

- 日期：2026-07-15
- 状态：已接受
- 背景：竞彩发现页面同时提供官方让球、销售状态和多种玩法 SP，长期保存成本较低，但会扩大模型输入边界。
- 决定：将官方 SP 作为独立观察保存；V1 模型仍只使用欧赔、亚洲让球、大小球及其多公司变化。
- 理由：保留未来研究价值，同时维持当前纯多公司市场基线的可解释边界。
- 影响：任何使用竞彩 SP 的模型实验都需要新增决策，不得静默加入特征。
- 替代方案：完全不保存官方 SP；会永久丢失前瞻时间线，未采用。

## D-015：提前进入阶段 3 标准化数据库

- 日期：2026-07-16
- 状态：已接受
- 背景：采集器单次验证和连续运行中期证据已经证明数据契约可落盘，项目负责人决定不再等待完整 7 天和 30 天周期后才建设分析库。
- 决定：允许立即实施阶段 3 PostgreSQL schema、迁移、可重放导入和 as-of 查询；阶段 1、2 验证继续并行运行。
- 理由：数据库层可以从不可变文件事实重建，提前建设不会改变原始数据，也能尽早暴露字段和查询问题。
- 影响：该决定覆盖 D-011、D-013 和执行计划中“验收完成后才进入数据库”的时间顺序，但不把任何未完成验收视为通过；采集器仍不得标记为生产可用，模型和 Web 门禁不解除。
- 替代方案：等待 30 天验收后再建库；项目负责人明确不采用。

## D-016：准备迁移到阿里云杭州单机 ECS

- 日期：2026-07-16
- 状态：部分替代，操作系统和数据盘现状被 D-017 替代
- 背景：本地采集和 PostgreSQL 数据库已经形成可运行闭环，项目负责人希望评估迁移到阿里云服务器以减少本地机器长期运行约束。
- 决定：准备迁移到阿里云杭州 `cn-hangzhou` 单台 ECS，首版使用 Ubuntu 24.04 LTS x86_64、2 vCPU / 4 GiB、40 GB 系统盘、100 GB ESSD PL0 数据盘，同机运行采集器和 PostgreSQL 17；备份采用私有 OSS 与本地 G 盘迁移副本。当前不部署 RDS、Docker、Web、域名或负载均衡。
- 理由：单机 ECS 足以支撑当前单进程采集和可重建分析库，部署复杂度低；杭州地域便于使用 OSS 内网 Endpoint 和国内网络环境验证。
- 影响：需要实现精确窗口报告、Linux/systemd 运行、健康检查、OSS 风格内容寻址备份、云端 smoke test 和回滚规则。正式切换后 7 天和 30 天验收重新计时，本地与云端运行时间不得拼接。该决定部分替代 D-007 的地区假设和 D-013 的“首月仅本地运行”假设，但不解除采集器生产、模型或 Web 门禁。
- 替代方案：继续本地运行到 30 天验收后再迁移；直接使用 RDS/Docker/负载均衡；暂不采用。

## D-017：采用实际 ECS 并分离 smoke 与正式存储

- 日期：2026-07-16
- 状态：部分替代，正式数据盘采购时点被 D-018 暂缓
- 背景：实际已创建的杭州 ECS 为 Ubuntu 22.04.5、2 vCPU / 4 GiB、40 GB 单系统盘且无 swap；NTP 正常并可访问 500。约 22 小时本地事实层已增长 54.3 MB，40 GB 无法承担 1 至 2 年原始数据、标准化记录和 PostgreSQL。
- 决定：接受实际 ECS 作为云端基线，不重装系统；系统盘仅运行隔离 smoke。配置 2 GiB swap，并在正式 systemd timer 启用前增加至少 100 GB ESSD 数据盘。正式数据、PostgreSQL 和备份暂存进入 `/srv/football-cups`，systemd 和采集器同时强制挂载门禁。Ubuntu 22.04 在 2027-03-31 前完成 Ubuntu 24.04/Python 3.12 迁移复审。
- 理由：先验证实际云端网络和解析能力，同时避免系统盘容量不足或数据盘掉线造成事实层写入错误位置。
- 影响：smoke 数据写入独立目录，不进入正式数据库或 7/30 天验收。OSS 必须完成真实上传、重新下载和 SHA-256 恢复，不能把同盘暂存目录称为备份。该决定部分替代 D-016 的 Ubuntu 24.04 和已具备数据盘假设，不改变杭州地域、单机 PostgreSQL 17、私有 OSS 或单写入者原则。
- 替代方案：直接在 40 GB 系统盘长期运行；需要本地清理和 OSS 归档状态机，复杂且增加恢复风险，未采用。

## D-018：暂缓购买 ECS 数据盘并维持本地正式运行

- 日期：2026-07-16
- 状态：已接受
- 背景：新增至少 100 GB ESSD 数据盘需要产生额外费用，项目负责人决定暂时不购买或挂载数据盘。
- 决定：阿里云 ECS 暂停正式迁移，只保留为隔离 smoke、部署验证和未来迁移预备环境。Windows 本地继续作为唯一正式采集写入者和数据库运行环境。没有正式数据盘前，不启用云端长期 timer，不安装正式 PostgreSQL 数据目录，不停止 Windows 采集。
- 理由：当前项目仍处于个人学习研究和数据积累阶段，避免在成本未确认前扩大云端投入；同时坚持 40 GB 系统盘不得承载长期事实层的安全边界。
- 影响：云端 7 天和 30 天验收不启动；阶段 3 数据库建设继续以本地环境为准。下一优先级回到本地采集连续性、异盘或网络备份、赛事格式登记和赛果闭环。
- 替代方案：立即购买数据盘并推进云端正式切换；暂不采用。

## D-019：采用全自动赛果确认与保守隔离

- 日期：2026-07-17
- 状态：部分替代，人工确认例外见 D-028
- 背景：日期直播页可以按 fixture ID、`status=4` 和固定比分节点确定性生成候选，但可能加时赛事的最终比分不能无条件视为 90 分钟比分；项目负责人要求后续不设置人工赛果确认步骤。
- 决定：赛果闭环采用自动采集、自动验证和自动隔离。日期直播页或其页面自身加载的日期 Full 数据流能够按唯一 fixture、状态 4 和固定比分字段确定结果时生成候选；只有候选与分析页比分一致、身份无冲突且赛事登记为 `regular_time_only` 时自动生成已验证赛果。可能加时、未知、冲突或无法证明口径的比赛保留证据并自动排除训练，不创建人工赛果待办。
- 理由：保持长期无人值守能力，同时不牺牲 90 分钟标签红线。自动化可以选择不确认，不能以覆盖率为理由猜测结果口径。
- 影响：原“杯赛人工核验”和赛事格式人工待办被替代；`verify-results` 仅保留兼容性。正式 30 天赛果验收从修复版上线后重新计时，7 月 15 日的发现子系统 24 小时结论不受影响。阶段 4 的 500 场按切点和不同 fixture 分别统计。
- 替代方案：所有比赛人工确认；无条件自动确认所有完场比分；均不采用。

## D-020：Windows S4U 与 G 盘双层异盘备份

- 日期：2026-07-17
- 状态：部分替代，数据库任务登录类型被 D-021 替代
- 背景：Windows 继续作为唯一正式写入者，D 盘事实层需要能够在用户注销、系统重启或磁盘故障后恢复；当前 G 盘属于另一块物理 NVMe。
- 决定：采集、数据库导入、每日镜像和每周内容寻址备份统一使用 S4U。每日 03:30 增量复制到 `G:\football-cups-backup`，每周日 04:30 生成 SHA-256 内容寻址批次到 `G:\football-cups-oss-layout`。备份通过共享锁生成一致性快照，PostgreSQL 不做物理备份，使用文件事实层重放。
- 理由：镜像备份恢复速度快，内容寻址备份可以发现静默损坏；两者均在独立物理磁盘上且不引入云端费用。
- 影响：30 天验收要求备份新鲜度、真实定时批次恢复、S4U 注销和重启验证。由于 PostgreSQL 拒绝管理员组成员的令牌启动服务器，数据库任务使用专用非管理员 `football-cups-runner`；G 盘不可用或备份过期时健康状态失败，不得回落到 D 盘其他目录。
- 替代方案：只做手工复制、只备份 PostgreSQL、在同一物理磁盘保存副本；均不能满足事实层灾难恢复要求。

## D-021：数据库任务采用本机加密密码登录兼容路径

- 日期：2026-07-17
- 状态：已接受
- 背景：当前独立 Windows 主机实测拒绝管理员、SYSTEM 和本地标准账户为 `football-cups-runner` 注册 S4U 任务，即使账户已属于 `Users`、具有批处理登录权并设置随机密码。管理员账户 `lcz` 的 S4U 任务可以导入，但 PostgreSQL 会因其管理员组令牌而拒绝在停机或重启后启动。
- 决定：采集和两个备份任务继续使用 S4U；数据库任务使用专用非管理员 `football-cups-runner` 和 Task Scheduler `Password` 登录类型。配置脚本在内存中生成并轮换随机密码，Windows LSA 加密保存任务凭据；密码不得进入 XML、文件、Git、日志或聊天。脚本同时授予工作区、数据目录、`.env` 和虚拟环境基础 Python 的最小 ACL。
- 理由：该方式保留注销和重启后的无人值守能力，同时确保 PostgreSQL 由非管理员令牌启动。2026-07-17 实测停止 PostgreSQL 后，任务自动启动服务器、完成导入并返回 0。
- 影响：D-020 的“四个任务统一 S4U”被部分替代，但 G 盘双层备份、单写入者和注销/重启验收门禁不变。换机、重建虚拟环境或变更 Python 安装路径后必须重新运行账户配置脚本；不能复制或导出本机任务密码。
- 替代方案：继续使用管理员 S4U 账户无法自动启动 PostgreSQL；交互任务不能通过无人值守验收；Windows 服务或域账户留待未来架构调整。

## D-022：允许隔离的公开历史研究与离线市场基线

- 日期：2026-07-17
- 状态：已接受
- 背景：正式每切点500场和7/30天验收需要持续积累，项目希望使用2025年至当前的公开历史数据提前判断纯盘口方法是否值得继续；500历史页面不适合作为批量回抓来源。
- 决定：允许使用显式注册的 Football-Data 静态 CSV/XLSX 和已核验 K1 派生特征，在 `data/research/` 与 PostgreSQL `research` schema 中运行离线市场基线。历史记录固定为 `research_only=true`、`backfill=true`、`strict_backtest_eligible=false`、`cutoff_eligible=false`。禁止批量访问500历史页面和任何身份规避行为。
- 理由：公开静态文件能够以较低访问压力提供可重复的历史研究样本；物理与逻辑隔离可以提前验证方法，同时不破坏 observed-at 和正式切点门禁。
- 影响：D-009、D-010 的历史辅助路线得到可执行实现，但 D-015 和阶段4门禁不变。K1只作为 `derived_closing_features`，2026只称为非盲时间分离评估；研究结果不得写入正式模型或供 Web/API 使用。D-029 另行允许基于冻结研究产物生成隔离的 research-only 影子概率，但不得追认为正式当前预测能力。
- 替代方案：等待前瞻样本自然达到门禁后再做任何方法研究；批量回抓500历史页；均未采用。

## D-023：盘口标准化 V2 与模型资格分层

- 日期：2026-07-17
- 状态：已接受
- 背景：500 中文盘口 HTML 被 `ISO-8859-1` 错误优先解码，部分亚盘公司名和盘口线乱码；旧采集层资格只证明市场请求和切点成功，不能证明模型所需字段完整。
- 决定：采用追加式盘口标准化 V2。页面按 meta、HTTP 声明、GB18030、UTF-8 的顺序解码；欧赔保留工作簿解析，亚盘、大小球和让球指数改为 HTML 直解析。`SnapshotBatch.strict_eligible` 保留旧采集含义，模型资格由独立评估决定，三个核心市场各自至少需要 3 家不同的完整 bookmaker。历史修复只从已有 blob/manifest 离线重放，不访问网络、不覆盖 V1、不伪造切点。
- 理由：原始事实层不需要改写即可纠正派生解析，同时能把网络采集成功、来源字段可用和模型严格准入分别审计，避免乱码或空盘口进入未来模型。
- 影响：新增 `MarketNormalization`、`SnapshotEligibilityAssessment`、`HandicapIndexRow`，迁移 006/007 和 V2 current/as-of 视图。让球指数和竞彩 SP 仍不进入 V1 模型。已通过的 24 小时发现验证与赛果子窗口继续有效；盘口 V2 的 7 天技术子窗口和 30 天切点资格子窗口从 2026-07-17 17:56:56 Asia/Shanghai 重新计时。
- 替代方案：覆盖 V1 JSONL、继续依赖亚盘/大小球 Excel 导出、仅按市场请求成功判断模型资格；均不采用。

## D-024：赛果补偿采用赛事 ID 口径登记与分析页双端点 fallback

- 日期：2026-07-20
- 状态：已接受
- 背景：500 竞彩发现页部分 `data-*` 身份字段在来源页面中已经呈现乱码，单靠赛事中文名无法稳定命中赛事格式登记；同时少数普通联赛比赛在日期直播页和 Full 数据流中持续缺失，但 `shuju` 与 `ouzhi` 分析端点均可解析出一致比分。
- 决定：`competition-formats.json` 支持 `competition_ids` 映射，SQLite 同步时先按赛事名、再按 `competition_id` 判断 `regular_time_only`、`may_have_extra_time` 或 `unknown`。当直播页和 Full 数据流都遗漏 fixture，且 `shuju-<fixture_id>` 与 `ouzhi-<fixture_id>` 两个分析端点都解析出同一比分时，可生成候选；只有赛事登记为 `regular_time_only` 且无身份冲突、比分冲突时才自动生成已验证赛果。
- 理由：赛事 ID 比乱码中文名更稳定；分析页双端点 fallback 解决来源清单裁剪造成的普通联赛结果缺口，同时仍要求两个端点一致和赛事口径明确。
- 影响：该 fallback 只用于补偿直播源遗漏，不改变杯赛和未知赛事自动隔离规则。不能解析出比分、仍显示 `VS` 或双端点不一致的比赛继续保持缺失或隔离，不得人工补造。
- 替代方案：把乱码赛事名直接写入配置、使用单个分析页生成赛果、无条件验证所有候选；均不采用。

## D-025：无效场次采用追加式逻辑排除

- 日期：2026-07-20
- 状态：已接受
- 背景：fixture `1358414` 经项目负责人在中国体彩竞彩足球赛果页面核验为无效场次；该 fixture 已关联多轮发现、盘口和质量事实，且部分原始页面同时包含其他正常比赛，物理删除会破坏 append-only、哈希审计和数据库重放。
- 决定：无效、取消或未结算场次追加 `fixture_invalidated/excluded` 质量事件并停止其待执行任务，从赛果覆盖分母、模型资格、训练、回测和阶段门禁中排除。人工只能确认无效状态并保存来源证据，不得人工补写比分；原始响应、manifest、JSONL 和既有盘口记录永久保留。
- 理由：逻辑排除达到不使用无效数据的目的，同时保留采集器当时真实观察和来源异常证据。
- 影响：新增幂等 `invalidate-fixture` 命令、SQLite 重建恢复规则和数据库迁移 `008_fixture_invalidation`。`result_cancelled` 与 `fixture_invalidated` 均从当前资格视图排除。
- 替代方案：物理删除 fixture 相关记录；因会连带共享原始证据并破坏可重放性，拒绝采用。

## D-026：接入中国体彩官方 90 分钟赛果证据源

- 日期：2026-07-20
- 状态：已接受
- 背景：中国体育彩票竞彩足球赛果页明确展示“全场比分（90分钟）”并说明包含伤停补时；它可以解决欧战、世界杯等赛事在 500 来源中无法自动证明 90 分钟口径的问题。
- 决定：500 继续作为比赛发现和盘口主源；中国体彩官方仅作为赛果 scope 与比分确认源。采集器新增 `SportteryScopeEvidence`、`SportteryInventoryBatch`、`SportteryFixtureLink` 和 `SportteryResultObservation`。只有官方页面 scope 可见、赛果清单分页完整、`match_number`/开球时间/主客顺序精确映射、`getMatchHeadV1` 与 `getFixedBonusV1` 比分一致且无身份冲突时，才可自动生成 `VerifiedResult`。
- 理由：官方页面提供明确 90 分钟口径，详情接口可为 `sportteryMatchId`、队伍、开球和比分提供独立一致性证据；分层保存能审计映射失败、阻断、未开奖、取消和来源冲突。
- 影响：新增迁移 `009_sporttery_result_evidence`、`reconcile-results --source sporttery --dry-run/--apply`、`sporttery-smoke` 和 `audit-result-evidence`。500 未确认口径候选与官方 90 分钟比分不一致时产生 `result_scope_difference`，但不阻止接受官方比分；只有明确 90 分钟来源之间比分冲突才产生 `result_conflict` 并隔离。官方来源失败不计入 500 盘口采集失败率。赛果来源 7 天和 30 天子窗口从该能力部署后重新观察。
- 替代方案：继续人工查询官方页或把官方结果人工写入比分；前者不能形成可重放证据，后者违反“不人工补写比分”边界，均不采用。

## D-027：体彩官方赛果采用低频调度与失败证据隔离

- 日期：2026-07-20
- 状态：部分替代，补偿处理范围被 D-028 扩展
- 背景：标准 headless Edge 已能保存中国体彩官方页面的90分钟 scope，但 `getUniformMatchResultV1` 和 `getMatchHeadV1` 在真实运行中返回 EdgeOne 567，官方页面自身的 XHR 也因相同响应触发 CORS；直接把空清单解释为 fixture 缺失会污染映射审计。
- 决定：将体彩官方补偿接入 `run-once`，默认每24小时最多一次，只处理开球后24小时至8天内仍未验证的 fixture。567、CORS、验证码、HTTP 非成功或空的不完整清单统一保存为来源失败，不生成 fixture 缺失或官方比分。精确映射引用文件事实层中按 `observed_at DESC, record_id DESC` 排序的第一条无乱码 `FixtureIdentity.record_id`；审计只有在完整官方候选到 `VerifiedResult` 引用链存在时返回 `ok`。
- 理由：低频自动重试可以在官方来源恢复后无需人工介入，同时保持访问压力可控；失败证据与结果事实分离，避免为了覆盖率绕过 WAF 或放宽90分钟证明。
- 影响：新增迁移010、官方补偿调度游标、指定 fixture smoke 和引用完整性审计。官方失败不改变500主采集退出码，也不重置既有500赛果验收窗口；体彩官方来源自身的7天/30天子窗口只能从首次完整成功后计时。D-028 后，已有负责人声明但尚无自动证据的 fixture 仍在8天窗口内继续官方补偿。
- 替代方案：使用代理、隐身插件、Cookie/Token重放或验证码处理；违反项目访问边界。把不完整清单记为映射缺失或手工填写比分；破坏证据语义。均不采用。

## D-028：允许项目负责人直接确认现有候选的90分钟口径

- 日期：2026-07-20
- 状态：已接受，部分替代 D-019 的完全禁止人工确认规则
- 背景：少量欧战和世界杯候选已由500双页面一致证明比分，但自动来源暂时无法证明其一定是90分钟口径；项目负责人已经独立核验并希望直接更新确认状态。
- 决定：项目负责人可明确声明一个或一批现有唯一候选属于常规时间。稳定命令只能读取候选比分，不能输入或修改比分；声明追加为 `project-owner-manual-declaration`，固定 `evidence_level=self_attestation`、`attestor_id=project-owner` 并引用候选。全部 fixture 先预检，任一不合格则整批零写入。合格声明可进入严格标签和阶段4计数。
- 理由：人工声明只解决赛果口径，不重复构建CSV、截图或证据包；候选引用、固定身份、不可变结果和质量事件足以审计系统内发生的声明。`self_attestation` 明确表示它不能证明外部页面真实性。
- 影响：人工覆盖率与自动覆盖率分别报告，人工确认不改善体彩接口成功率或30天自动赛果验收。体彩8天补偿继续运行；后续官方结果一致时追加自动证据，不一致时产生冲突并将比赛移出严格资格。旧 `manual`/`manual-import` 若无完整候选引用不得进入严格视图。
- 替代方案：继续永久隔离全部无法自动证明的候选；要求CSV、截图和独立证据目录；均不采用。

## D-029：允许隔离盘口影子预测

- 日期：2026-07-20
- 状态：已接受
- 背景：正式阶段 4 仍需每切点至少 500 场严格前瞻样本，等待周期较长；D-022 的公开历史研究已经证明 closing 1X2 去水市场具备初步研究价值，项目负责人希望提前观察当前比赛概率输出形态。
- 决定：允许在 `data/research/` 和 PostgreSQL `research` schema 中生成 research-only 当前比赛影子预测。影子预测只输出 90 分钟胜/平/负概率，使用 `devig-consensus-v1`：历史多公司 closing 1X2 单公司去水后取分量中位数共识，当前比赛只读取正式库中已选定的 live V2 合格批次及其精确欧赔快照。模型产物标记 `research_kind=model_artifact/backfill=true`，影子发布标记 `research_kind=shadow_event/backfill=false`。
- 理由：可以低成本观察概率稳定性、发布窗口和后续赛果评估链路，同时不伪装为正式模型或 Web 能力。
- 影响：新增迁移012、研究模型数据集/版本/激活/影子预测/评估表、`football-cups-research train-model` 与 `shadow-predict` 命令，以及独立 Windows 任务 `FootballCups-Research-Shadow-Prediction`。同一 `(channel, fixture_id, target, prediction_cutoff)` 只能发布一次；错过窗口只能追加 abstention，不允许历史补发。影子指标必须按切点、模型、赛事、自动赛果和全部有效赛果分开报告，不计入阶段4门禁，不对外提供 Web/API，也不输出投注金额、组合、收益或 ROI。
- 替代方案：继续只等待正式 500 场门禁；立即开发正式模型或预测页面；均未采用。

## D-030：赛事类型与市场证据等级分离

- 日期：2026-07-21
- 状态：已接受
- 背景：首次四场 research-only 盘口复盘显示，芬超等低证据赛事和强客队热门需要比高覆盖赛事更保守的置信表达；现有影子预测只有赛事名称和概率，不能按稳定赛事ID隔离评估，也使用了未限制截止时间的最新身份。
- 决定：新增显式赛事注册表，将 `competition_type` 与 `market_evidence_tier` 分离。分层不得改变去水概率，只限制置信、增加风险或触发 abstain。A/B/C级可发布，C级最高 low，D级、分层冲突和截止前身份缺失必须 abstain。所有初始等级均为 provisional；同一赛事、同一切点至少200场自动验证评估且跨度至少90天后才可人工复审 high 门禁。
- 理由：赛事类型、赛果口径和盘口证据成熟度不是同一概念；显式ID、as-of身份和版本化双哈希可以避免名称猜测、未来信息泄漏和策略漂移，同时保留低级联赛研究价值。
- 影响：新增迁移013、`research-competition-profiles.json`、确定性公司分歧/方向强度/置信规则及分层影子评估。旧预测保持不可覆盖并按 legacy 分类；策略升级只影响未来自然发布，不解除阶段4或Web门禁。
- 替代方案：低级联赛全部禁用、按名称自动猜测、直接修改概率或自动升降级；均未采用。

## D-031：韩职 K1 规则护栏采用 shadow 优先和人工启用

- 日期：2026-07-21
- 状态：已接受
- 背景：项目负责人希望利用 K1 历史盘口差值增加“防收割”能力。现有330场 closing 派生特征可用于发现跨市场候选结构，但2025 chronological OOS和2026 retrospective walk-forward均未通过，旧0-7累加评分也没有证明市场概率之外的稳定风险。
- 决定：建设仅适用于 `competition_id=16` 的 research-only 规则护栏。规则在同一 target、同一 `SnapshotBatch`、截止前证据中先按同公司配对计算欧赔概率、亚洲盘、大小球和让球指数差值，再以硬完整性、结果方向冲突和辅助风险的确定性动作矩阵产生 `keep/caution/downgrade/abstain`。首版只追加 shadow 拟议动作，不修改基础概率、置信、风险标签或发布状态。
- 理由：盘口差值和跨市场冲突值得前向验证，但历史两点数据无法证明实时轨迹或因果“收割”。规则矩阵比任意总分更可解释，也能对每个动作追溯到公司、快照、阈值和截止时间。
- 影响：已新增版本化配置、迁移014、独立 assessment、completed manifest门禁和按切点/规则前向评估。330场历史复现确认热门校准残差为正且区间跨0，不产生启用资格。达到自动赛果样本、90天跨度、分批稳定性、block-bootstrap和Holm校正门槛仍只产生 `review_eligible`；必须新增负责人决策和未来生效的新策略版本，才允许实际降置信或 abstain。人工声明赛果不解除启用门禁。本决策不解除7天、30天、阶段4或Web门禁。
- 替代方案：直接启用旧评分、训练小样本分类器或永久只做报告；前两者证据不足，后者不满足最终护栏需求，均不采用。
